<Page x:Class="CER.Foundry.game"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:rpg="clr-namespace:CER.Rpg;assembly=Rpg"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="game">
    <Grid x:Name="View" DataContext="{Binding}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Margin="5">gm_name</TextBlock>
        <TextBox x:Name="something" Grid.Row="1" Margin="5" Text="{Binding gm_name, Mode=TwoWay}"/>
    </Grid>
</Page>
